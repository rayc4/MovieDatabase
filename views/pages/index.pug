html
	head
		style.
			.image {
				max-width: 100%;
				max-height: 30vh;
				margin: auto;
			}
	body(align="center")
		include ../partials/header
		h3 Welcome to the Movie Database. 
		p Here are some random movies to get you started.
		each movie in shuffled
			a(href="movies/"+movie._id)
				img(src=movie.poster alt=movie.title class="image")

